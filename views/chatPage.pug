extends layouts/main-layout.pug

block content
    if errorMessageChat 
        span.errorMessageChat #{errorMessageChat}
    else
        script. 
            var chatId = '!{chat._id}';

block scripts
    script(src="/js/chatPage.js")